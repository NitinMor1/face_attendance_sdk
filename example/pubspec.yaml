name: face_recognition_kit_example
description: "Example app for face_recognition_kit."
publish_to: 'none'

version: 1.1.0

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  face_recognition_kit:
    path: ../
  provider: ^6.1.5+1
  intl: ^0.20.2
  path_provider: ^2.1.5
  path: ^1.9.1
  shared_preferences: ^2.5.5
  camera: ^0.12.0+1
  camera_web: ^0.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
